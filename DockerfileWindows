# --- Stage 1: Build Frontend ---
FROM node:20-windowsservercore-ltsc2022 AS frontend-builder
WORKDIR /app/frontend
COPY frontend/package*.json ./
RUN npm install
COPY frontend/ ./
RUN npm run build

# --- Stage 2: Final Image ---
FROM python:3.11-windowsservercore-ltsc2022

WORKDIR /app

# Set PowerShell as default shell for setup
SHELL ["powershell", "-Command", "$ErrorActionPreference = 'Stop'; $ProgressPreference = 'SilentlyContinue';"]

# Install FFmpeg
# Note: This downloads the essentials build from gyan.dev
RUN Write-Host "Downloading FFmpeg..." ; \
    Invoke-WebRequest -Uri "https://www.gyan.dev/ffmpeg/builds/ffmpeg-release-essentials.zip" -OutFile "ffmpeg.zip" ; \
    Write-Host "Extracting FFmpeg..." ; \
    Expand-Archive -Path "ffmpeg.zip" -DestinationPath "C:\ffmpeg_temp" ; \
    $ffmpegDir = Get-ChildItem -Path "C:\ffmpeg_temp" -Filter "ffmpeg-*" | Select-Object -First 1 ; \
    Write-Host "Installing to System32..." ; \
    Move-Item -Path "$($ffmpegDir.FullName)\bin\*" -Destination "C:\Windows\System32" ; \
    Remove-Item -Path "C:\ffmpeg_temp" -Recurse -Force ; \
    Remove-Item -Path "ffmpeg.zip" -Force ; \
    Write-Host "FFmpeg installed successfully."

# Copy requirements and install
COPY backend/requirements.txt ./backend/requirements.txt
RUN pip install --no-cache-dir --upgrade -r ./backend/requirements.txt

# Copy backend code
COPY backend/ ./backend/

# Copy built frontend from Stage 1
COPY --from=frontend-builder /app/frontend/dist ./frontend/dist

# Set the working directory to backend for the CMD
WORKDIR /app/backend

# Default port for the application
ENV PORT=8000
EXPOSE 8000

# Run the application
CMD ["python", "main.py"]
